package com.guessaname.marvelapp.dao

import okhttp3.HttpUrl

object Request {
   /* val API_BASE_URL: HttpUrl = HttpUrl.parse("http://gateway.marvel.com/v1/public")
    private const val PATH_CHARACTER = "characters"

    // URL's params
    const val TIME_STAMP_KEY = "ts"
    const val HASH_KEY = "hash"
    const val API_KEY = "apikey"
    const val NAME_STARTS_WITH = "nameStartsWith"

    // limit is 100
    const val LIMIT = "limit"

    //Order the result set by a field or fields.
    // Add a "-" to the value sort in descending order.
    // Multiple values are given priority in the order in which they are passed.
    const val ORDER_BY = "orderBy"*/
}